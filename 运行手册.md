# 运行手册：风机 SCADA 三模型全量化实验（科研标准 SOP）

> ## 🟩🟩 2026-06-02 启动口径更新（权限门 + 唯一 v2 口径，覆盖下方"裸跑即全量"旧条款）
> **`python 启动.py`（不带参数）不再启动全量**：触发代码级权限门 → 安全退出 + 打印操作提示（零副作用：不写 manifest / 不预处理 / 不训练）。
> - **唯一正式全量命令**（90 run，需用户许可）：`python 启动.py --split chronological_v2 --cache-version v2 --confirm-full`
> - **免许可（无需 --confirm-full）**：`python -m pytest tests`、`python -m compileall …`、`python 启动.py --dry-run --split chronological_v2 --cache-version v2`、**单 farm 单 seed 1 epoch** `--smoke`。
> - **`--split` 默认 = `chronological_v2`**（唯一主口径，`feature_version=v2`/`cache_version=v2`）；`narrow_v1` 仅历史复现，需显式 `--split narrow_v1`，与 v2 物理隔离。
> - **`--force` / 多 farm/多 seed/多 epoch smoke / 90-run / 三 farm 全量预处理** 一律需 `--confirm-full`。
> - **结果隔离**：v2 的 dataset / scores / metrics.jsonl / checkpoint / manifest / logs 全部落 `实验结果(或实验状态)/chronological_v2__v2/…`。
> - 轻量验证三件套：`python -m compileall 启动.py 实验配置.py 实验工具.py SCADA数据集 Anomaly-Transformer-main TranAD-main TriTrackNet-main TCN-master tests` → `python -m pytest tests -q` → `python 启动.py --dry-run --split chronological_v2 --cache-version v2`。
> 下方 §SOP "点击运行默认 = 全量化 / 不带参数即跑 90 run" 等旧条款**以本 banner 为准**（已被权限门覆盖）。
>
> ---

> ## 🟥🟥 2026-05-30 协议更新（数据驱动温度异常检测，覆盖下方旧条款）
> **研究方向 = 基于风机 SCADA 的【广义温度异常检测 / 温度驱动故障早期预警】**（依据 = 数据/事实，详见 `PROJECT_ARCHITECTURE.md` 顶部 banner）。
>
> 下列 SOP 条款**替换**旧版（旧"13 列 / 1700 / zscore / train_positive=0 走 1700"作废）：
> 1. **不得硬编码通道**：通道由 `数据预处理.py` 数据驱动选出（全部温度通道 → NBM 残差 → 多准则选择）。`meta.json.cols` = 选定温度指标；`n_channels` 可变（各 farm 不同）。
> 2. **NBM 工况归一红线**：`T̂=f̂(wind,power,gen_rpm,rotor_rpm,ambient)` 只在 **train(label=0)** 拟合；val/test 仅 transform。残差 `e=T−T̂` 是异常指标，不是原始温度。
> 3. **标签红线**：正例 = `IEC 'Forced outage' | HOT Stopping==1 | Message/Status 含【强制停机·温度·传动链·润滑故障】关键词` 的事件 **±2h**（= `DatasetProtocol.POSITIVE_HALF_WINDOW_HOURS`）；**已剔除常规刹车(brake program)/手动停机/电网(grid loss)等与温度无关的停机**(审查 A1)；**逐机组隔离打标**，某机组事件不串扰池化中其它机组(审查 A3)；缺真实故障字段 → 全 0(不把所有事件当正例, A5)；**不再用轴承码 1700**。长缺口行标 ignore(−1)，不进评测。
> 4. **train 去污仍强制**：`meta.train_positive_count` 必须 = 0（删 label==1 行，无监督模型只学正常分布）。
> 5. **缺失处理红线**：禁止 `fillna(0)`；只允许限长插值（≤3 步=30min），更长缺口标 ignore。
> 6. **归一化 = RobustScaler**（中位/IQR，抗残差重尾），per-farm fit on train-healthy。
> 7. **选择只用 train+val**，test 全程不参与任何拟合/选择（防泄漏）。
> 8. **评测口径**：event-level F1 / AUC(Mann-Whitney) / lead-time（正例 <1%，禁用准确率为主指标）。
> 9. **`--use-pretrain` 已弃用**（跨 farm 通道不一致）；默认矩阵不含预训练。
> 10. **入口不变**：`python 启动.py`（默认三 farm × 三 baseline × 2 module × 5 seed）；预处理由 `数据预处理.py --farm <farm>` 数据驱动执行。
>
> 验证：`python 温度指标选择.py`（自检）、`python -m compileall ...`、`python -m pytest tests/ -v`、`python 启动.py --dry-run`。
>
> ---

> 本手册是项目的**操作标准作业程序（Standard Operating Procedure）**。
> **每次跑实验 / 每次改代码 / 每次写报告**都必须按本手册的清单逐项执行。
> 最近一次同步：2026-05-30（数据驱动温度异常检测重构；以上 banner 覆盖旧条款）。

---

## 🟥 SOP 强制清单（每次必做）

> ⚠️ 下列 8 项是**不可跳过**的科研标准。任何 PR / commit / 实验结果交付前必须全部完成。

### A. 启动配置（每次实验前）

1. **点击运行默认 = 全量化**：`python 启动.py` 不带参数即跑 `TrainingProtocol` 当前协议（90 model runs + 3 preprocess）。smoke 链路验证使用 full 模式单 farm 1-epoch：`python 启动.py --farms kelmarsh --module all --seeds 0 --epochs 1 --force`。
2. **启动 banner 必须显示**：mode / farms / baselines / modules / seeds / epochs / batch_size / 实验矩阵总数 / manifest 路径 / "5 秒内 Ctrl+C 取消"提示。banner 缺失 = 启动器代码被破坏。

### B. 训练过程输出（IDE 命令行三层）

3. **每 epoch 必须打印**两份内容：
   - 一行 `METRIC|model=...|phase=train/val|epoch=N|...|f1=...|auc=...` 机器可解析
   - 三行可读块：`[Model] phase=... epoch=...` + `Loss=... | Accuracy=... | Precision=... | Recall=... | F1=... | AUC=...` + `threshold=... | farm=... | module=... | seed=...`
4. **每个模型 run 结束必须打印** `┌── ★ MODEL FINAL ──┐` 方框块，含分类 (5 指标) + 回归 (3 指标, 仅 TriTrackNet) + 工程信息 (loss/threshold/farm/module/seed) 三段。
5. **所有 step 完成必须输出** `ALL_STEPS_FINISHED|batch=N` 末尾标志；任一失败立即 `FAILED|step=...|exit_code=...|log=...`。

### C. 科学性约束（不可妥协）

6. **训练数据零正例污染**：`数据预处理.py::run_full_farm_mode` 必须在 RobustScaler fit 之前剔除 `train_y_raw == 1` 的行。落盘 meta.json `train_positive_count` 必须 = 0，否则 fail。
7. **Val/Test 不参与阈值选择**：`choose_threshold_and_polarity_by_validation` 只看 val_labels + val_scores + train_scores（fallback）。Test 数据进入模型只在 final evaluation 一次。
8. **多 seed 报告必须 mean ± std**：单 seed 数字不可作为结论。当前正式协议为 seeds=(0,1,2,3,4)，TranAD seed 隔离修复 (`实验.py main()` 在 import 后重置 RNG) 保证多 seed 真不同。

---

## 🟧 工作流 SOP（按事件类型选）

### 事件 A：跑一次正式全量化实验

数据范围: 全部 3 farm (Kelmarsh + Penmanshiel + Hill of Towie), 全部年份 (2016-2024), 全部 turbines, 全量数据。

按时间顺序执行：

```text
[Step 1] git status / 文件状态确认
   - PROJECT_ARCHITECTURE.md 最新版
   - 实验状态/manifest.json 中无 status="running" 残留
   - 实验配置.py / 数据预处理.py / 三 实验.py 自上次实验后无修改 (或修改已审计)

[Step 2] 数据预处理确认
   - 三 farm meta.json 都存在
   - meta.json 中 train_positive_count = 0 (无污染)
   - cols = 数据驱动选定的温度指标 (由 温度指标选择.py 决定, 通道数可变)

[Step 3] 轻量验证 (不训练)
   python -m compileall -q 启动.py 实验工具.py 实验配置.py tests Anomaly-Transformer-main TranAD-main TriTrackNet-main TCN-master
   python -m pytest tests/ -v
   python 启动.py --dry-run
   验收: 单元测试通过; dry-run 显示 model_runs=90 / steps=93

[Step 4] 启动全量化
   python 启动.py
   (默认即跑 90 model runs, banner 5 秒内可 Ctrl+C 取消)
   预计耗时: 约 1.5~3 小时 (实测: 上一轮 90 run≈1:20; 本轮 RTX5090 上单 run 数十秒~数分钟,
            3 farm 预处理另算约 10~20min; 以 `python 实验监控.py` 的实时 ETA 为准, 旧"13-20h"估计作废)

[Step 5] 实时监控
   - IDE 命令行看 banner → 每 epoch 块 → 每 step 方框
   - 可平行: tail -f 实验结果/logs/batch{N}_*.log
   - 长任务必须每 10 分钟检查 manifest、最新日志时间、FAILED 行、GPU/进程活性和 ETA

[Step 6] 完成校验
   manifest.json 全部 status=success
   实验结果/metrics.csv 行数 ≥ 10 epochs × 3 phases × 90 model runs (约 2700)
   实验结果/{farm}/{baseline}/metrics.jsonl 9 份齐全

[Step 7] 生成报告 [已废弃 — 实验报告生成/ 目录已删除]
   python 实验报告生成/生成TCN全量对比报告.py --experiment-id EXP-001    [已废弃]
       → 实验报告生成/TCN对比实验报告_EXP-001.docx                      [已废弃]
   python 更新数据集分析报告.py          → SCADA数据集分析报告.docx

[Step 8] 多 seed 聚合
   对 metrics.csv 按 (farm, baseline, module, phase=test, epoch=final) 分组聚合 mean±std。
   TCN 达标口径: event-level F1 + percentile threshold，ΔeF1 ≥ 0.10 记为达标。任何未达标后的修改只能由 train/val 和训练稳定性分析驱动,
   不得用 test 指标反推超参数。
```

### 事件 B：改代码（任何源文件修改）

```text
[Step 1] 改前
   - 读相关代码 (5-段式注释帮你快速理解 修改原因/作用/数学原理/执行流程/科研标准)
   - 必要时 invoke superpowers skill (brainstorming / writing-plans / systematic-debugging)

[Step 2] 改时
   - 每处修改必须加 5-段式注释:
       # ============================================================
       # 名称: <函数/段名>
       # 修改原因: 为什么改 (引用 bug/finding/需求来源)
       # 作用: 这段代码做什么
       # 数学原理: 涉及的数学公式 (无则写 "无")
       # 执行流程: 1. ... 2. ... 3. ...
       # 科研标准: 与项目研究目标的关系 / 不可破坏的不变量
       # ============================================================
   - 修改要 minimal,一个 commit 一个原因

[Step 3] 改后立即验证
   python -c "import ast; ast.parse(open('改的文件.py', encoding='utf-8').read()); print('OK')"
   python -m compileall -q 启动.py 实验工具.py 实验配置.py tests Anomaly-Transformer-main TranAD-main TriTrackNet-main TCN-master
   python -m pytest tests/ -v
   python 启动.py --dry-run
   (本轮只做轻量验证; 需要正式实验前再另行跑 full 模式单 farm smoke)

[Step 4] 静态扫描 + 关系图更新 (大改动后)
   python 静态扫描.py     → 更新 静态扫描结果.json
   python 生成关系图.py   → 更新 关系图.docx
   阅读 关系图.docx §5 看新增/解决的 finding

[Step 5] 文档同步
   - 更新 PROJECT_ARCHITECTURE.md 对应章节 (加 修改日期 + 原因 + 影响)
   - 若新增/删除文件,更新 §17 目录树
   - 若改了科学逻辑,更新本手册 §SOP 强制清单

[Step 6] 清理
   find . -name "__pycache__" -type d -exec rm -rf {} +
```

### 事件 C：发现 bug

按 systematic-debugging skill 四阶段：

```text
Phase 1 根因调查 (找根因前不写代码)
   - 读完整错误栈
   - 找最小复现步骤
   - git diff / file mtime 看最近改了什么
   - 多组件 (loader → preprocess → baseline) 系统加诊断 log

Phase 2 模式分析
   - 找同 repo 内类似工作代码做参照
   - 列出 working vs broken 的所有差异 (不要假设"那不重要")

Phase 3 假设 + 单变量测试
   - 写一个明确假设
   - 改最小一行验证
   - 不工作 → 形成新假设 (不堆改)

Phase 4 实现修复
   - 先写失败测试 (TDD)
   - 改一处源代码 root cause
   - 重跑测试 + smoke

如果 3+ 次修复失败 → 停下质问架构 (不要继续打补丁)
```

### 事件 D：清理冗余

按 2026-05-25 §17 已经建立的清理标准：

```text
- 死代码 (无入边 + 非 entry point + 非 test) → 删
- 第三方 demo (locuslab/TCN 等本项目未用的子目录) → 删
- 失效测试 (import 失败) → 删
- 一次性脚本 (功能已应用到目标文件) → 删
- 历史 metrics (路径已被新结构替代) → 删
- __pycache__ + .ipynb_checkpoints → 删 (自动重生)
- 已删文件在 PROJECT_ARCHITECTURE.md §15 / §17 留删除记录
```

---



---

## 0. 一句话定位

**点击 `启动.py` 即跑全量化** = 3 farm × 3 baseline × 2 module × 5 seed = **90 个正式 run**，加 3 个预处理 step，共 93 步。每 epoch 在 IDE 命令行打印 loss/accuracy/precision/recall/F1/AUC；每个模型 run 结束打印整体指标方框。

**注意：`--use-pretrain` 已于 2026-05-30 弃用**（跨 farm 通道不一致，无法池化）。如需预训练请使用按 farm 单独预训练+微调流程，不在默认矩阵中。

---

## 1. 启动方式

### 1.1 点击运行（默认 = 全量化）

| IDE | 操作 |
|---|---|
| VS Code | 打开 `启动.py` → 右上角 ▶ Run Python File |
| PyCharm | 打开 `启动.py` → Run > Run '启动' |
| 命令行 | `python 启动.py` |

默认参数（在 `启动.py::parse_args` 写死）：
- `--mode full`
- `--farms kelmarsh,penmanshiel,hill_of_towie`
- `--models anomaly,tranad,tritrack`
- `--module all` （= baseline_only + tcn_input_residual）
- `--seeds 0,1,2,3,4`
- `--epochs 10`
- `--batch-size 128`

### 1.2 子集运行（按需）

```bash
# Smoke 链路验证（约 5 分钟）
python 启动.py --farms kelmarsh --module all --seeds 0 --epochs 1 --force

# 只跑 Kelmarsh + 全模块全 seed
python 启动.py --farms kelmarsh

# 只看实验矩阵不真跑
python 启动.py --dry-run

# 只跑 +TCN 这一组（baseline_only 已跑完时增量补 tcn）
python 启动.py --module tcn_input_residual

# 只跑单一 baseline
python 启动.py --models tranad
```

### 1.4 启动后立即看到的 banner

```text
==============================================================================
  风机 SCADA 三 baseline + TCN 全量化对照实验  —  一键启动器
==============================================================================
  mode        : full
  farms       : ['kelmarsh', 'penmanshiel', 'hill_of_towie']  (|farms|=3)
  baselines   : ['anomaly', 'tranad', 'tritrack']  (|baselines|=3)
  modules     : ['baseline_only', 'tcn_input_residual']  (|modules|=2)
  seeds       : ['0', '1', '2', '3', '4']  (|seeds|=5)
  epochs      : 10    batch_size: 128
  pretrain    : False  (window=2020-10-01 00:00~2020-11-30 23:50)
  实验矩阵    : 3 × 3 × 2 × 5 = 90 runs (+ 3 个 preprocess step)
  manifest    : E:\创新\实验状态\manifest.json
  metrics CSV : E:\创新\实验结果\metrics.csv
  → 若需取消请在 5 秒内 Ctrl+C
==============================================================================
```

如果矩阵不是你想要的，立刻 Ctrl+C 取消。

---

## 2. IDE 命令行输出格式（科研可读 + 机器可解析）

### 2.1 每 epoch 输出（每个 farm × baseline × module × seed 各 5 个 epoch）

每 epoch 在 train / val 各打印两次。**train 块**示例：

```text
METRIC|model=AnomalyTransformer|phase=train|epoch=2|accuracy=0.4540|precision=0.0000|recall=0.0000|f1=0.0000|auc=0.7016|...
  [AnomalyTransformer] phase=train  epoch=2
  Loss=-23.0607 | Accuracy=0.4540 | Precision=0.0000 | Recall=0.0000 | F1=0.0000 | AUC=0.7016
  threshold=-0.0000 | threshold_source=validation_evt1_f1 | score_polarity=negative | farm=kelmarsh | module=baseline_only | seed=0
```

第一行是机器解析格式（METRIC| ... |key=value| ...）；后三行是人类可读多列对齐。

### 2.2 每个模型 run 结束输出（每个 step 一个总结方框）

```text
┌────────────────────────────────────────────────────────────────────────────┐
│  ★ MODEL FINAL  step=kelmarsh_anomaly_transformer_baseline_only_seed0       │
├────────────────────────────────────────────────────────────────────────────┤
│  分类: accuracy=0.931522  precision=0.070796  recall=1.000000  f1=0.132231  auc=0.916239 │
│  信息: loss=-0.005542  threshold=-0.000000  threshold_source=validation_evt1_f1 │
│  信息: score_polarity=negative  farm=kelmarsh  module=baseline_only        │
│  信息: seed=0                                                              │
└────────────────────────────────────────────────────────────────────────────┘
```

### 2.3 全部 run 结束 / 中途失败信号

```text
ALL_STEPS_FINISHED|batch=0           # 全部 57 步成功
# 或
FAILED|step=...|exit_code=1|log=...  # 任一步失败立即停 + 指向日志
```

---

## 3. 科研流程执行标准（每次都做）

按这个清单逐项确认，再启动正式 run。

### 3.1 启动前（必做）

- [ ] **数据预处理已就绪**：检查三 farm 的 `meta.json` 都存在
  ```bash
  ls "Anomaly-Transformer-main/dataset/SCADA_kelmarsh/meta.json"
  ls "TranAD-main/processed/SCADA_penmanshiel/meta.json"
  ls "TriTrackNet-main/dataset/SCADA_hill_of_towie/meta.json"
  ```
  缺失时 `启动.py` 会自动在 build_steps 加入 preprocess 步骤。
- [ ] **事件清单已扫描**：`SCADA数据集/事件分布扫描/事件分布_{farm}.csv` 三份齐全（用于切片调优）
- [ ] **代码版本固定**：本次实验对应 PROJECT_ARCHITECTURE.md §17 的提交点；如本地有修改，先记录 git/file SHA
- [ ] **环境一致**：`E:\ancoda\chuangxin\python.exe` 含 `torch==2.11.0+cu130`，由 `resolve_model_python` 自动校验
- [ ] **manifest 状态干净**：`实验状态/manifest.json` 中无 `status=running` 残留（上次中断未清理）；如有，先 `--force` 重置

### 3.2 启动时（自动）

启动器按下列顺序串行执行 step（每一步成功才进下一步）：

1. preprocess_{kelmarsh}  → 落盘 SCADA_kelmarsh/
2. preprocess_{penmanshiel}
3. preprocess_{hill_of_towie}
4. kelmarsh_anomaly_transformer_baseline_only_seed{0,1,2,3,4}
5. kelmarsh_anomaly_transformer_tcn_input_residual_seed{0,1,2,3,4}
6. kelmarsh_tranad_baseline_only_seed{0,1,2,3,4}
7. ...（笛卡尔积展开，共 90 个 model run）

每步成功后立即写 `manifest.json` 的 `status=success`，可断点续传。

### 3.3 运行中（实时监控）

- 命令行看 banner → 每 step 进入 `========== RUN xxx ==========` → 每 epoch 看 METRIC + 可读块 → 每 step 看方框 → 下一 step
- 日志文件：`实验结果/logs/batch{N}_full_normal_{step_key}.log`
- **只读监控器**（2026-05-30 新增）：`python 实验监控.py` 打印进度报告块（步骤进度 / 已用时长 / 各 baseline 平均每步 / 剩余步骤 ETA / 预计完成时刻 / 当前 run 最新 METRIC / bug 扫描）；`python 实验监控.py --compact` 输出单行 `PROGRESS|...` 摘要，适合每 5 分钟周期采样汇报。全程只读，不碰任何实验产物。
- 中断：Ctrl+C 安全中断；下次 `python 启动.py` 自动 SKIP 已成功的 step

### 3.4 运行后（每次都做）

- [ ] **检查 ALL_STEPS_FINISHED**：终端最后一行必须是 `ALL_STEPS_FINISHED|batch=N`，否则有 step 失败
- [ ] **核对 manifest**：
  ```bash
  python -c "import json; m=json.load(open('实验状态/manifest.json',encoding='utf-8')); print(sum(1 for k,v in m.items() if not k.startswith('_') and v.get('status')=='success'), '/', len([k for k in m if not k.startswith('_')]), 'success')"
  ```
- [ ] **横向汇总表**：`实验结果/metrics.csv` 含 farm 列，行数 ≥ 10 epochs × 3 phases × 90 model runs （≈ 2700 行）
- [ ] **生成报告**：[已废弃 — 实验报告生成/目录已删除] `python 实验报告生成/生成报告.py` 不再可用
- [ ] **多 seed 标准差**：在 metrics.csv 上对 (farm, baseline, module, phase=test, epoch=final) 聚合 mean ± std，写入实验报告
- [ ] **失败 step 排查**：若 manifest 中有 `status=failed`，读对应 log，按 `关系图.docx §5` 排查

### 3.5 跨次实验的可比性约束

- **同切片**：`实验配置.py PerFarmSplitProtocol` 决定，不在 CLI 改
- **同通道 schema**：由 `温度指标选择.py` 数据驱动决定（每 farm 的 `meta.json.cols` + `n_channels`），通道数可变
- **同 epoch / batch / seeds**：默认参数已锁定，不轻易改
- **任何修改必须在 PROJECT_ARCHITECTURE.md 记录** 修改日期 + 修改原因 + 影响范围

---

## 4. 产物目录树

```text
E:\创新\
├── 实验状态\
│   ├── manifest.json                  # 每 step 状态; --resume 用
│   └── batch_history\                 # 旧 batch 归档
├── 实验结果\
│   ├── metrics.csv                    # 横向汇总 (含 farm 列)
│   ├── logs\batch{N}_full_normal_{step_key}.log
│   └── {farm}\{baseline}\
│       └── metrics.jsonl              # 该 (farm, baseline) 的所有 epoch + final 指标
├── Anomaly-Transformer-main\dataset\SCADA_{farm}\
│   ├── SCADA_{train,val,test}.npy
│   ├── SCADA_{train,val,test}_label_raw.npy
│   ├── SCADA_test_label.npy
│   └── meta.json                      # cols, split, norm_params, positive_count
├── Anomaly-Transformer-main\checkpoints\{farm}\{formal,smoke}\{module}\seed{N}\
│   └── SCADA_checkpoint.pth
└── (TranAD/TriTrackNet 对称结构)
```

---

## 5. 输出指标定义（写报告时用）

| 指标 | 计算 | 意义 |
|---|---|---|
| **Loss** | minimax KL (AT) / two-phase MSE (TranAD) / SmoothL1 (TriTrackNet) | 训练目标 |
| **Accuracy** | (TP+TN) / (TP+TN+FP+FN) | 含负例多时偏高，需结合 F1 |
| **Precision** | TP / (TP+FP) | 报警准确度（少误报） |
| **Recall** | TP / (TP+FN) | 故障捕获率（少漏报） |
| **F1** | 2·P·R / (P+R) | 精召和谐均值，二分类首选 |
| **AUC** | ∫ ROC 曲线下面积 | 阈值无关，对极性鲁棒；< 0.5 提示分数极性反 |
| **MSE / MAE / RMSE** | 仅 TriTrackNet 预测子任务 | 预测残差的统计量 |
| **threshold** | 在 val 上选 F1 最大对应的分数阈值 | 由 `choose_threshold_and_polarity_by_validation` 输出 |
| **threshold_source** | `validation_evt1_f1` (Val 含事件) / `fallback_low_f1` (Val 不可分) | 阈值来源透明化 |
| **score_polarity** | `positive` / `negative` | 异常分数方向 |

---

## 6. 已知"科研标准"陷阱（必读）

| 项目 | 标准 | 当前实现 |
|---|---|---|
| Train 不能含正例 | unsupervised baseline 假设 train = p(X\|normal) | ✅ 数据预处理.py 已剔除 train 弱标签正例（Hill of Towie 20.87% → 0%） |
| Val 必须含事件 | 阈值校准前提 | ✅ `PerFarmSplitProtocol.{KELMARSH,PENMANSHIEL,HILL_OF_TOWIE}` 切片定义保证 |
| Test 独立于阈值选择 | 防泄漏 | ✅ `choose_threshold_and_polarity_by_validation` 只看 val |
| Seeds 各异 | 多 seed 取 mean±std | ✅ TranAD §10.4 seed 隔离已修复，五 seed 真正不同 |
| 极性投票约束 | Val 双方向 F1 都 < 0.05 时退 fallback | ✅ `实验工具.py` 已加 MIN_VALID_F1=0.05 阈值 |
| 通道 schema 一致 | 三 farm 输入张量通道由数据驱动决定 | ✅ 温度指标选择.py 按 farm 独立选择，meta.json.cols 记录 |
| 报告每个 epoch 指标 | 训练过程可视化 | ✅ print_epoch_block 每 epoch 输出 6 项 + 工程信息 |
| 报告每个模型 final | 模型间横向对比 | ✅ print_metrics_summary 输出方框总结 |

---

## 7. 快速链路验证（5 分钟 smoke）

> 🟥 2026-05-30 更新：旧 `--mode quick` (legacy T06_2021 / dataset/SCADA 布局) **已弃用并失效**——
> 数据预处理重构为数据驱动 full-farm 流程后只产出 `SCADA_{farm}/`，legacy 路径不再生成
> `dataset/SCADA/` 与 `processed/SCADA/T06_2021_*.npy`，故 `--mode quick` smoke 会在第一个模型 run
> 报 `FileNotFoundError: dataset/SCADA`。**链路验证改用 full 模式单 farm 1-epoch smoke**（走的就是
> 正式实验的真实路径），下方命令已验证可跑通 7/7：

```bash
python 启动.py --farms kelmarsh --module all --seeds 0 --epochs 1 --force
```

期待：
- 7 个 step 全部 success（1 preprocess + 6 model runs）
- 看到完整的 banner + 每 epoch METRIC + 每 epoch 可读块 + 每 step 方框总结
- 终端最后 `ALL_STEPS_FINISHED|batch=N`

通过即可启动正式 `python 启动.py`（默认全量化）。

> 注：`test_tranad_seed_propagates` 旧版用 `--smoke`(legacy) 路径，随 legacy 弃用而失败已知；
> 该 seed 隔离保证现由 `tests/test_make_labels.py` 回归验证，并在正式 5-seed 矩阵中天然体现。

---

## 8. 相关文档索引

| 文档 | 内容 |
|---|---|
| `PROJECT_ARCHITECTURE.md` | 项目主架构 + 各阶段进度（§16=全量化，§17=审计） |
| `温度异常检测_预处理与指标选择.md` | 数据驱动预处理 + NBM 残差 + 多准则温度指标选择 |
| `SCADA数据集/SCADA数据集分析报告_齿轮箱轴承温度方向.docx` | 三 farm 全量数据统计 |
| `基线借鉴说明.md` | 基线借鉴 #6 预训练流程说明 |
| `实验监控.py` | 只读进度/时间/ETA/bug 监控器（`--compact` 单行摘要供 5min 周期采样） |
| 本手册 `运行手册.md` | 操作流程与每次执行的标准 checklist |
