# 项目架构 — 风机 SCADA 温度异常【真实故障早期预警(real_fault_wl)】检测

> 当前权威架构文档 (2026-07-24 更新)。取代根目录 `PROJECT_ARCHITECTURE.md`(层叠历史 banner, 已过时)。
> 主口径: chronological_v2 + feature_version=v2 + **real_fault_wl** (消息级白名单真实故障早警, Tier-1过温∪Tier-2传动链热, 72h合并+事后24h ignore, lead=72步)。
>
> **2026-07-24 主口径切换**: A′ 过温代理标签已【彻底删除】(`make_overtemp_earlywarning_labels`/`多视野标签.py`/`视野前沿.py` 及 2 测试删除, 归档 `docs/archive/pre_realfault_switch_20260724/`), 默认标签切 real_fault_wl; 评价以【事件级 F1(affiliation/range)+ CARE(event-recall/Earliness/FAR)+ AUPRC】为主表。S6 新增 MI-AMO-BHS 混合特征选择(`混合特征选择.py`, 环境变量 `FS_MODE=hybrid` opt-in, 默认仍无监督, 待事件级消融证优再翻默认)。详见 `docs/superpowers/specs/2026-07-24-*`。

## 0. 数据流总图
```
原始SCADA CSV (Kelmarsh/Penmanshiel/HOT, ~156GB)
   │  SCADA数据集/loaders/*.py  (per-farm加载, 列发现, 事件流)
   ▼
SCADA数据集/数据预处理.py  (S0-S9 主流水线)
   物理剪+Hampel去尖峰 → 限长插值 → NBM工况归一残差(温度−f(工况))
   → S6选通道: 无监督去冗余(温度指标选择.py) 或 MI-AMO-BHS混合特征选择(混合特征选择.py, FS_MODE=hybrid)
   → real_fault_wl真实故障早警标签(消息级白名单, train去污) → RobustScaler
   ▼  落盘 train/val/test.npy + *_labels.npy + train_sup.npy + event_table.csv + timestamps/turbines侧车 + meta.json (×4模型目录)
   ▼
4 base模型入口 {AT/TranAD/TriTrackNet/wt}/实验.py
   读产物 → 切窗 → 训练(无监督重构/预测) → 异常分数
   × 模块: baseline_only / tcn_input_residual(输入级α门控TCN)
   ▼  落盘 scores (ResultLayout)
   ▼
评测: 实验工具.py(val选阈/极性) + 事件指标.py(affil/range F1)
后处理: 卡尔曼分数平滑评价.py(输出分数级KF, 零重训) / 集成(tools/diagnostics/p3_ensemble_eval.py)
编排: 启动.py / 全量编排.py(矩阵+断点续传); 集成评价.py(跨模型集成)
```

## 1. 核心模块(原理·作用·数据流·功能·必需性)

### SCADA数据集/loaders/__init__.py + {kelmarsh,penmanshiel,hill_of_towie}.py 【必需】
- 原理: 抽象基类 FarmLoader + per-farm 子类; Greenbyte 单CSV / HOT 多表JOIN。
- 数据流: (turbine,year) → _load_one(原始DataFrame) + _load_labels(事件流) → load_pooled 池化+时间窗裁剪。
- 功能: 统一输出 (data, events); `SCADA_LOAD_NJOBS` 限并行防大CSV(2.8-3.7GB) OOM。

### SCADA数据集/数据预处理.py 【必需·核心】
- 原理: NBM 工况归一 e=T−f(工况)(残差才度量"同工况偏置"); A′标签=残差持续越train分位→过温事件, 正例=事件前H步。
- 数据流: loaders → 物理剪+Hampel(物理边界+鲁棒去尖峰) → 限长插值(≤3步) → NBM残差(train-healthy拟合) → 无监督去冗余选通道 → A′标签(make_overtemp_earlywarning_labels) → train去污 → RobustScaler → 落盘+meta契约校验。
- 功能: 唯一数据生产入口; `--label-mode overtemp_a1 --a1-theta-pct/sustain/lead`; validate_meta_contract 落盘前 fail-fast 防泄漏。

### 温度指标选择.py 【必需(瘦身后)】
- 原理: NormalBehaviorModel(HistGBR逐通道, 工况+滞后特征) 产残差; select_temperature_indicators_unsupervised(方差→VIF→|ρ|聚类)无监督去冗余。
- 现状: 旧多准则监督选择(Boruta/熵权/RF)已证近空操作→标 DEPRECATED 暂留(测试依赖); 管线用无监督版。
- 功能: NBM残差生成 + 通道选择。

### 事件指标.py 【必需】
- 原理/功能: Tatbul range-F1 + Huet affiliation-F1 (主表) + false_alarms/day; point_adjust_f1 仅附录。纯函数无泄漏。

### 实验工具.py 【必需·共享】
- 功能: 统一 ignore(-1)过滤 / choose_threshold_and_polarity_by_validation(仅val选阈+极性) / compute_binary_metrics / augment_event_metrics / regression_metrics(预测式分数=正残差能量) / record_and_print_metric(写metrics.jsonl+csv) / wandb离线。
- 防泄漏关键点: 阈值/极性只在val选; orient_scores 接受fallback极性后缀。

### {AT/TranAD/TriTrackNet/wt-transformer}/实验.py 【必需·4基线】
- AT/TranAD: 重构式(重建误差=异常分数)。TriTrackNet: 多步预测(96→24)+RevIN。wt: 预测式(全18通道新息正能量)。
- 共契约: --farm/module/seed/epochs/bs/lr/scheduler/split-id/feature-version/output-dir; 读PerFarmPaths产物→训练→落scores→统一metrics。
- 模块: baseline_only / tcn_input_residual(SCADATCNResidualAdapter, α门控残差)。
- TriTrack OOM已修(sliding_window_view视图+_LazyWindowDataset按批物化)。wt out_dim=D全通道预测(防单通道分数塌缩退化)。

### 实验配置.py 【必需·单一真源】
- 内容: DatasetProtocol(窗口WIN_AT/TRANAD/WT/SEQ_LEN) + ChronologicalSplitProtocol(train16-21/val22/test23-24) + PerFarmPaths(4模型×farm产物路径) + ResultLayout(版本化结果路径) + TCNProtocol + SlidingWindow + GridSearchProtocol + SchedulerProtocol。

### 启动.py / 全量编排.py 【必需·编排】
- 功能: 4模型×模块×farm×seed 矩阵展开 + 断点续传 + 权限门(全量需--confirm-full)。--models 默认含 wt。

### 集成评价.py 【必需】 跨模型集成评分(ResultLayout读分数)。
### 卡尔曼滤波/卡尔曼滤波.py 【必需】 kalman_filter_1d(1D分数平滑), 被 卡尔曼分数平滑评价.py + 集成引用。
### 卡尔曼分数平滑评价.py 【必需·后处理】 对任意落盘分数 val网格选KF参数→test评一次, 零重训, module记"{module}+kalman"。

### tools/diagnostics/ 【可复用保留3个】
- p3_ensemble_eval.py(--farm 泛化集成评测) / p4_attribution.py(per-channel报警归因) / probe_temp_label_availability.py(标签可得性)。其余一次性探针已清理(结论存 docs/A1战役结果汇总)。

### tests/ 【必需】 pytest 套件(预处理契约/无泄漏/指标/极性/A′标签等)。

## 2. 当前状态(诚实)
- kelmarsh: A′(99/3/72)全战役完成, 最优=集成 test点F1 0.582/AUC0.798/AUPRC0.612 (停机口径0.05的11.6×)。
- penmanshiel: 泛化成立(集成val+AUPRC两农场最优); HOT未做。
- 防泄漏/非point-adjust 全程守。诚实天花板≈0.58-0.65(无监督); 监督头(A1, 进行中)目标更高。

## 3. 已知坑
- 长后台训练跨Claude会话重启易被孤儿杀(用单进程+短跑规避)。
- 大CSV(2.8-3.7GB×464列)加载OOM: SCADA_LOAD_NJOBS=1/2 限并行; 根治=usecols(未做)。
- Remove-Item 对中文路径+递归被harness误拦, 用 [System.IO.Directory]::Delete 绕过。
